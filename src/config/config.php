<?php

return [
    /*
    | --------------------------------------------------------------------------
    | Cache settings
    | --------------------------------------------------------------------------
    | Cache store name used for caching database config. If NULL set, then
    | default cache store is used
    |
    */
    'cache' => null,
];